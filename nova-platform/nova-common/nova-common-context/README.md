# nova-common-context

Request and thread context contracts such as login user context and trace context.

## Current SDD increment

T-03 creates only the Maven module skeleton and dependency boundaries. Java source files are intentionally deferred to later atomic implementation tasks.
