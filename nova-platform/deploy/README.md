# deploy

Deployment assets directory.

No SQL or Docker files are generated in `T-01`. SQL scripts, Docker Compose configuration, and environment assets will be added only in their dedicated later tasks.
