# nova-business

`nova-business` is the top-level Maven aggregation module for business centers.

## Current strict SDD scope

Only the following business center is allowed in the current stage:

- `nova-system-center`

Forbidden business centers such as finance, AI, file, notification, job, and workflow are intentionally not created.
